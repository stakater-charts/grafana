apiVersion: v1
kind: Secret
metadata:
  labels:
{{ include "labels.stakater" . | indent 4 }}
{{ include "labels.chart" . | indent 4 }}
  name: {{ template "name" . }}-credentials
data:
  user: {{ .Values.grafana.secret.config.user }}
  password: {{ .Values.grafana.secret.config.password }}
